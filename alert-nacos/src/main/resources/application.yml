logging:
  level:
    com.neusoft.mid.msf: DEBUG
    com.alibaba: ERROR

server:
  port: 18889

management:
  endpoint:
    health:
      show-details: always

push:
  address: 127.0.0.1:9100

alert:
  nacos:
    groups: CMS,MASS,DEFAULT_GROUP
